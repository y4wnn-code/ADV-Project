DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=bloggwatch_dev

# Copy this file to `next-app/.env` and fill in credentials.
# Then run migrations: `node scripts/migrate.js` (from `next-app/`).
# After migrations run this import script from repository root:
# `node next-app/scripts/import_db.js`
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=changeme
DB_NAME=bloggwatch_dev

APP_PORT=3001
